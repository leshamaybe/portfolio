import React from 'react';
import MainContainer from '@/components/container/MainContainer';
import WorkExp from '@/components/screens/works/WorkExp';

const works = () => {
    return (
        <MainContainer>
            <WorkExp />
        </MainContainer>
    );
};

export default works;
